import styled from "@emotion/styled";

const FavoriteBox = styled.div`
	padding: 0.5rem 1rem;
`;

const Favorite = () => {
	return <FavoriteBox>User Favorite</FavoriteBox>;
};

export default Favorite;
