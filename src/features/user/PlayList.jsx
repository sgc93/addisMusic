import styled from "@emotion/styled";

const PlayListBox = styled.div`
	padding: 0.5rem 1rem;
`;

const PlayList = () => {
	return <PlayListBox>User PlayList</PlayListBox>;
};

export default PlayList;
