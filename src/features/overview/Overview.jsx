import styled from "@emotion/styled";

const OverviewBox = styled.div`
	padding: 0.5rem 1rem;
`;

const Overview = () => {
	return <OverviewBox>Home Overview</OverviewBox>;
};

export default Overview;
